name: ci

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  cpp-quality:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@v4
  package-compliance:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@v4
  deb-build-and-smoke:
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - image: ghcr.io/xgc-team/xgc2-images/xgc2-build-bionic-dev:1.0.0
            distribution: bionic
            runner: ubuntu-latest
          - image: ghcr.io/xgc-team/xgc2-images/xgc2-build-focal-dev:1.0.0
            distribution: focal
            runner: ubuntu-latest
          - image: ghcr.io/xgc-team/xgc2-images/xgc2-build-jammy-dev:1.0.0
            distribution: jammy
            runner: ubuntu-latest
          - image: ghcr.io/xgc-team/xgc2-images/xgc2-build-noble-dev:1.0.0
            distribution: noble
            runner: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@v4


      - name: Run C++ quality gate
        run: |
          set -euo pipefail
          docker pull ghcr.io/xgc-team/xgc2-images/xgc2-build-focal-dev:1.0.0
          docker run --rm --network none \
            -v "${GITHUB_WORKSPACE}:/work" -w /work \
            ghcr.io/xgc-team/xgc2-images/xgc2-build-focal-dev:1.0.0 \
            bash -lc './.xgc2/scripts/check_cpp_quality.sh'


      - name: Check whitespace
        run: git -C "${GITHUB_WORKSPACE}" diff --check HEAD

      - name: Package compliance
        run: |
          set -euo pipefail
          cd "${GITHUB_WORKSPACE}"
          ./.xgc2/scripts/check_package_compliance.sh


      - name: Build deb and run installed smoke test
        run: |
          set -euo pipefail
          install -d .ci/build-manifests
          docker run --rm --network none \
            -e PACKAGE_DISTRIBUTION="${{ matrix.distribution }}" \
            -v "${GITHUB_WORKSPACE}:/work" \
            -w /work \
            "${{ matrix.image }}" \
            bash -lc '
              set -euo pipefail
              export DEBIAN_FRONTEND=noninteractive
              for pkg in \
                build-essential \
                ca-certificates \
                cmake \
                dpkg-dev \
                fakeroot \
                file \
                git \
                libeigen3-dev \
                libgtest-dev
              do
                if ! dpkg -s "${pkg}" >/dev/null 2>&1; then
                  echo "image is missing ${pkg}; use xgc2-build-*-dev" >&2
                  exit 1
                fi
              done
              git config --global --add safe.directory /work
              ./.xgc2/scripts/check_package_compliance.sh
              ./.xgc2/scripts/build_deb.sh
              dpkg -i ./debs/*.deb
              ./.xgc2/scripts/smoke_test_installed.sh
            '

      - name: Create trusted build manifest
        run: |
          product_version="$(awk -F': *' '/^version:/ {print $2; exit}' .xgc2/product.yml)"
          for architecture in amd64 arm64; do
            python3 .xgc2/scripts/xgc2_artifact_manifest.py build \
              --deb-dir debs \
              --output-dir .ci/build-manifests \
              --product libxgc2-math-dev \
              --product-version "$product_version" \
              --distribution "${{ matrix.distribution }}" \
              --architecture "$architecture" \
              --source-sha "$GITHUB_SHA" \
              --ci-run-id "$GITHUB_RUN_ID" \
              --ci-workflow "$GITHUB_WORKFLOW" \
              --ci-workflow-ref "$GITHUB_WORKFLOW_REF"
          done

      - name: Upload deb artifacts
        uses: actions/upload-artifact@v4
        with:
          name: libxgc2-math-dev-${{ matrix.distribution }}-all
          path: |
            debs/*.deb
            .ci/build-manifests/*.json
          retention-days: 14
          include-hidden-files: true
